services:
  jenkins:
    # image: myjenkinskit
    image: jenkins/jenkins:lts-jdk21
    container_name: myjankinshost
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock 
      - /Users/mdsharif/backupJenkins/jenkins_home:/var/jenkins_home
      # - /var/run/docker.sock:/var/run/docker.sock
      # - /usr/bin/docker:/usr/bin/docker
